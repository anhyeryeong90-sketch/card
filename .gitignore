# Supabase 설정 파일 (보안상 제외)
supabase-config.js

# 환경 변수 파일
.env
.env.local
.env.*.local

# 빌드 생성 파일 (로컬에서만)
# supabase-config.js는 빌드 시 생성되므로 .gitignore에 유지

# 로그 파일
*.log

# 운영체제 파일
.DS_Store
Thumbs.db

# 에디터 설정
.vscode/
.idea/
*.swp
*.swo
